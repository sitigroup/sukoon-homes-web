"use client";

/**
 * Admin-only score factor breakdown. Not used on public Homes pages.
 * Kept as a no-op so old imports cannot render point weights to users.
 */
export default function SukoonTrustBreakdownCards() {
  return null;
}
